#include "lists.h"

/**
 *free_listint2 - frees list
 *@head: pointer pointer head
 *Return: none
 */

void free_listint2(listint_t **head)
{
	listint_t *temp;

	if (head == NULL)
		break;

	while (*head != NULL)
	{
		temp = *head;
		*head = (*head)->next;
		free(temp);
	}
}
