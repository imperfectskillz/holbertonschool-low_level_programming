#include "sort.h"

/**
 *bubble_sort- function that sorts using bubble
 *@array: integers
 *@size: size of array
 *Return: none
 */

void bubble_sort(int *array, size_t size)
{
	int temp;
	int i;
	int j;
	int flag = 0;

	j = size - 1;
	while (flag != 1)
	{
		
	
