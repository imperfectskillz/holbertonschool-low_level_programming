#include "holberton.h"
#include <stdio.h>

/**
 *rev_string - rev
 *@s: tested
 *Return: something
 */

void rev_string(char *s)
{
	int n = 0;
	int result*;

	while (s[n] != '\0')
	{
		n++;
	}
	n = n - 1;
	while (n >= 0)
	{
		result = result + s[n];
		n--;
	}
	return (result);
}
